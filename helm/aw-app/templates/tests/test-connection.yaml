apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aw-app.fullname" . }}-test-connection"
  labels:
    {{- include "aw-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aw-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
